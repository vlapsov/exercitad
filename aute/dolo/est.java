if (action instanceof Rotate) {
    // Code to be executed if action is an instance of Rotate
} else if (action instanceof AnotherClass) {
    // Code to be executed if action is an instance of AnotherClass
} else {
    // Code to be executed if action is not an instance of Rotate or AnotherClass
}
